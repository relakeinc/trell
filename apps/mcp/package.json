{
  "name": "@trell/mcp",
  "version": "0.0.0",
  "description": "Trell MCP server — analytics and actions for AI assistants and bots. Pure logic; runners live in @trell/api.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no linter yet\" && exit 0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@trell/shared": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vite": "^7.0.0",
    "vitest": "^4.1.11"
  }
}
